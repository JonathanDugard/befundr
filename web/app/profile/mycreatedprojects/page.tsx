import MyCreatedProjects from '@/components/mycreatedprojects/mycreatedprojects';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return <MyCreatedProjects />;
};

export default page;
